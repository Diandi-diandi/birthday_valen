name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]  # 👈 當你推送到 main 分支時觸發

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies 🔧
        run: npm install

      - name: Build 🏗️
        run: npm run build
        # 如果這邊失敗，我們可以在 Actions 裡看到詳細原因

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite 打包後會產生 dist 資料夾
          branch: gh-pages # 把打包結果推送到 gh-pages 分支
